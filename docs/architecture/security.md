# Security boundary

Security owns capability checks, approval policy, command/path policy, and secret-safe
presentation. The host supplies a verification command adapter that connects approved typed
verification commands to Runtime execution; Security does not spawn processes or write storage.
