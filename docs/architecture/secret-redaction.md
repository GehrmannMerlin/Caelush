# Secret redaction

Secret-safe presentation is applied at the Security boundary. The canonical Tool result path
requires an explicit `ToolResultSanitizerPort`; raw Tool output is not a public event or model
contract, and redaction never grants permission or changes durable execution facts.
